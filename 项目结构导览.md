# Quasar 项目结构导览

> 仓库：chased9166/Quasar（基于 master 分支）  
> 语言：C# / .NET Framework 4.5.2+  
> 定位：开源 Windows 远程管理工具（Remote Administration Tool）

---

## 一、根目录总览

```
Quasar/
├── Quasar.sln                 # Visual Studio 解决方案文件，包含全部子项目
├── README.md                  # 项目简介、功能列表、编译说明
├── CHANGELOG.md               # 版本更新记录
├── CONTRIBUTING.md            # 贡献者指南
├── ROADMAP.md                 # 未来规划（Web API、跨平台、CLI 服务器等）
├── LICENSE                    # MIT 开源许可证
├── appveyor.yml               # AppVeyor CI 持续集成配置
├── .gitignore / .gitattributes
├── .github/
│   └── ISSUE_TEMPLATE/        # GitHub Issue 模板
├── Images/                    # README 截图资源
├── Licenses/                  # 第三方依赖的许可证文本
├── Quasar.Common/             # 【公共库】客户端与服务端共用代码
├── Quasar.Common.Tests/       # 【单元测试】针对 Common 库的测试
├── Quasar.Client/             # 【被控端】部署在目标机器上的客户端程序
└── Quasar.Server/             # 【控制端】管理员操作界面（Windows Forms GUI）
```

---

## 二、四大核心子项目

### 1. `Quasar.Common` — 公共基础库

客户端与服务端**共用**的代码，不含任何界面或平台特定逻辑。

```
Quasar.Common/
├── Cryptography/
│   ├── Aes256.cs              # AES-256 加密/解密（通信加密核心）
│   ├── Sha256.cs              # SHA-256 哈希
│   └── SafeComparison.cs      # 防时序攻击的安全比较
├── DNS/
│   ├── Host.cs                # 主机地址模型
│   ├── HostsManager.cs        # 多主机管理（轮询重连）
│   └── HostsConverter.cs      # 主机列表序列化/反序列化
├── Enums/                     # 枚举定义（连接状态、文件类型、鼠标动作等）
├── Extensions/                # 扩展方法（Socket、String、DriveType）
├── Helpers/
│   ├── FileHelper.cs          # 文件路径、大小处理工具
│   ├── PlatformHelper.cs      # 平台版本检测
│   └── StringHelper.cs        # 字符串工具
├── IO/
│   └── FileSplit.cs           # 大文件分块传输逻辑
├── Messages/                  # 所有网络消息定义（Protocol Buffers 序列化）
│   ├── IMessage.cs            # 消息接口
│   ├── IMessageProcessor.cs   # 消息处理器接口
│   ├── MessageHandler.cs      # 消息路由/分发
│   ├── MessageProcessorBase.cs# 处理器基类
│   ├── TypeRegistry.cs        # 消息类型注册表
│   ├── Do*.cs                 # 指令类消息（如 DoShellExecute、DoMouseEvent）
│   ├── Get*.cs                # 查询类消息（如 GetProcesses、GetDirectory）
│   ├── Set*.cs                # 状态设置消息（如 SetStatus）
│   ├── FileTransfer*.cs       # 文件传输相关消息
│   └── ReverseProxy/          # 反向代理专用消息
├── Models/                    # 数据模型（Drive、Process、FileSystemEntry 等）
├── Networking/
│   ├── ISender.cs             # 发送接口抽象
│   ├── PayloadReader.cs       # 数据包读取（帧解析）
│   └── PayloadWriter.cs       # 数据包写入（帧封装）
├── NativeMethods.cs           # Windows API P/Invoke 声明
├── Utilities/
│   ├── ByteConverter.cs       # 字节转换工具
│   └── SafeRandom.cs          # 密码学安全随机数
└── Video/
    ├── Codecs/UnsafeStreamCodec.cs  # 视频流编解码（远程桌面传输）
    ├── Compression/JpgCompression.cs # JPEG 压缩
    └── Resolution.cs                # 分辨率模型
```

**关键设计**：所有网络消息均定义在此处，使用 **protobuf-net** 进行二进制序列化，配合 AES-256 + TLS 实现加密通信。

---

### 2. `Quasar.Client` — 被控端（客户端程序）

运行于目标 Windows 机器上的轻量级后台程序，响应服务端下发的指令。

```
Quasar.Client/
├── Program.cs                 # 入口点：初始化应用、启动单实例互斥锁
├── QuasarApplication.cs       # 应用主体：连接管理、生命周期控制
├── Config/
│   └── Settings.cs            # 编译期配置（服务端 IP、端口、密钥、安装路径等）
├── Networking/
│   ├── Client.cs              # TCP 客户端（连接、断线重连、数据收发）
│   └── QuasarClient.cs        # 应用层客户端（握手、身份认证）
├── Messages/                  # 各功能模块的消息处理器（Handler）
│   ├── ClientServicesHandler.cs    # 客户端服务控制（卸载、重连、更新）
│   ├── FileManagerHandler.cs       # 文件管理（上传、下载、删除、重命名）
│   ├── KeyloggerHandler.cs         # 键盘记录
│   ├── MessageBoxHandler.cs        # 远程消息框弹出
│   ├── NotificationMessageProcessor.cs  # 通知消息
│   ├── PasswordRecoveryHandler.cs  # 密码恢复
│   ├── RegistryHandler.cs          # 注册表操作
│   ├── RemoteDesktopHandler.cs     # 远程桌面截图推流
│   ├── RemoteShellHandler.cs       # 远程 Shell（cmd）
│   ├── ReverseProxyHandler.cs      # SOCKS5 反向代理
│   ├── ShutdownHandler.cs          # 关机/重启/休眠
│   ├── StartupManagerHandler.cs    # 启动项管理
│   ├── SystemInformationHandler.cs # 系统信息采集
│   ├── TaskManagerHandler.cs       # 进程管理
│   ├── TcpConnectionsHandler.cs    # TCP 连接查看
│   └── WebsiteVisitorHandler.cs    # 远程打开网站
├── Logging/
│   ├── Keylogger.cs           # 全局键盘钩子、按键捕获
│   └── KeyloggerService.cs    # 键盘记录后台服务
├── Recovery/
│   ├── IPassReader.cs         # 密码读取器接口
│   ├── Browsers/              # 浏览器密码提取（Chrome、Edge、Firefox、Opera、Brave、Yandex、IE）
│   └── FtpClients/            # FTP 客户端密码提取（FileZilla、WinSCP）
├── Registry/
│   ├── RegistryEditor.cs      # 注册表增删改
│   └── RegistrySeeker.cs      # 注册表遍历/搜索
├── ReverseProxy/
│   └── ReverseProxyClient.cs  # SOCKS5 反向代理客户端实现
├── Setup/
│   ├── ClientInstaller.cs     # 客户端安装逻辑（复制文件、设置属性）
│   ├── ClientStartup.cs       # 开机自启配置（注册表/计划任务）
│   ├── ClientUninstaller.cs   # 卸载逻辑
│   └── ClientUpdater.cs       # 自动更新
├── User/
│   ├── UserAccount.cs         # 当前用户账户信息（用户名、权限级别）
│   └── ActivityDetection.cs   # 用户活动状态检测（在线/离开）
├── Helper/                    # 客户端专用工具类（屏幕、系统信息、注册表等）
├── IO/
│   ├── BatchFile.cs           # 批处理脚本生成（用于自删除/更新）
│   ├── HardwareDevices.cs     # 硬件设备枚举
│   └── Shell.cs               # Shell 进程封装
├── IpGeoLocation/             # IP 地理位置查询（通过外部 API）
├── Extensions/                # 客户端专用扩展方法
├── Utilities/
│   ├── NativeMethods.cs       # 客户端专用 Windows API 声明
│   └── SingleInstanceMutex.cs # 单实例互斥锁（防止重复运行）
└── ILRepack.targets           # ILRepack 配置（将依赖合并进单一 EXE）
```

**关键特性**：
- 使用 **ILRepack** 将所有依赖合并为单一可执行文件，方便部署。
- `Config/Settings.cs` 在调试模式下直接使用硬编码配置；发布模式下由服务端客户端生成器注入。

---

### 3. `Quasar.Server` — 控制端（服务器 / 管理界面）

管理员运行的 **Windows Forms** 图形界面程序，用于监控并控制所有连接的客户端。

```
Quasar.Server/
├── Program.cs                 # 入口点：初始化 TLS 证书、启动服务器
├── Networking/                # 服务端网络层（监听、连接管理）
├── Forms/                     # WinForms 界面窗体
│   ├── FrmMain.*              # 主界面：客户端列表、全局操作
│   ├── FrmBuilder.*           # 客户端生成器（配置并编译客户端）
│   ├── FrmCertificate.*       # TLS 证书管理
│   ├── FrmRemoteDesktop.*     # 远程桌面查看窗口
│   ├── FrmRemoteShell.*       # 远程 Shell 交互窗口
│   ├── FrmFileManager.*       # 远程文件管理器
│   ├── FrmTaskManager.*       # 远程任务管理器
│   ├── FrmKeylogger.*         # 键盘记录日志查看器
│   ├── FrmRegistryEditor.*    # 远程注册表编辑器
│   ├── FrmPasswordRecovery.*  # 密码恢复结果查看
│   ├── FrmConnections.*       # 远程 TCP 连接查看
│   ├── FrmAbout.*             # 关于对话框
│   └── FrmRegValueEdit*.      # 注册表值编辑器（多种类型）
├── Messages/                  # 服务端消息处理器
├── Build/
│   ├── ClientBuilder.cs       # 客户端构建逻辑（修改配置后重新编译/打包）
│   └── Renamer.cs             # 程序集混淆/重命名（使用 Mono.Cecil）
├── Controls/                  # 自定义 UI 控件
│   ├── HexEditor/             # 十六进制文件编辑器控件
│   ├── RapidPictureBox.cs     # 高刷新率图像显示控件（用于远程桌面）
│   ├── RegistryTreeView.cs    # 注册表树形视图控件
│   └── ListViewEx.cs          # 扩展 ListView 控件
├── Models/                    # 服务端数据模型（客户端状态等）
├── ReverseProxy/              # 服务端反向代理管理
├── Registry/                  # 注册表操作辅助
├── Enums/                     # 服务端枚举（传输类型等）
├── Extensions/                # 服务端专用扩展方法
├── Helper/                    # 服务端工具类
└── Utilities/                 # 服务端通用工具
```

---

### 4. `Quasar.Common.Tests` — 单元测试

对公共库核心功能的单元测试，使用 **NUnit** 框架。

```
Quasar.Common.Tests/
├── Compression/
│   └── JpgCompression.Tests.cs    # JPEG 压缩/解压测试
├── Cryptography/
│   ├── Aes256.Tests.cs            # AES-256 加解密正确性测试
│   └── SHA256.Tests.cs            # SHA-256 哈希测试
└── Helpers/
    └── FileHelper.Tests.cs        # 文件工具方法测试
```

---

## 三、通信协议与加密架构

```
服务端 (Quasar.Server)
    │
    │  TLS 1.2 加密传输层
    │  ├─ 握手：X.509 自签名证书
    │  └─ 数据：AES-256 加密的 Protobuf 消息帧
    │
客户端 (Quasar.Client)
```

- **帧格式**：`PayloadWriter` / `PayloadReader` 负责帧的封包与解包（长度前缀）。
- **序列化**：使用 **protobuf-net** 将消息对象序列化为紧凑二进制格式。
- **消息路由**：`TypeRegistry` 维护消息类型 → 处理器的映射，`MessageHandler` 负责分发。
- **加密**：`Aes256` 在 Protobuf 序列化之上再做对称加密；`SafeComparison` 防止时序攻击。

---

## 四、构建与 CI

### 本地构建

1. 使用 **Visual Studio 2019+**，安装「.NET 桌面开发」工作负载。
2. 打开 `Quasar.sln`，还原 NuGet 包（`nuget restore`）。
3. 选择构建配置：
   - **Debug**：直接使用 `Quasar.Client/Config/Settings.cs` 中的硬编码配置，可直接运行测试。
   - **Release**：使用服务端的「客户端生成器」(`FrmBuilder`) 配置并生成客户端。
4. 构建输出位于 `Bin/` 目录。

### CI（AppVeyor）

配置文件：`appveyor.yml`

```yaml
image: Visual Studio 2019
configuration: [Debug, Release]   # 两种配置并行构建
before_build:
  - nuget restore                  # 还原 NuGet 包
build:
  project: Quasar.sln
  parallel: true
artifacts:
  - path: Bin                      # 将 Bin 目录作为构建产物上传
```

- 每次推送自动触发 AppVeyor CI，同时构建 Debug 和 Release 两个配置。
- 构建成功后，`Bin/` 目录作为制品可供下载。

---

## 五、第三方依赖

| 依赖库 | 用途 |
|--------|------|
| **protobuf-net** | 网络消息的 Protocol Buffers 序列化 |
| **BouncyCastle** | TLS / 密码学扩展支持 |
| **Mono.Cecil** | 程序集读写（客户端混淆/重命名） |
| **ILRepack** | 将客户端所有依赖合并为单一 EXE |
| **GlobalMouseKeyHook** | 全局鼠标/键盘钩子（键盘记录功能） |
| **Open.NAT** | UPnP 自动端口映射 |
| **Be.HexEditor** | 十六进制编辑器控件 |
| **ResourceLib** | PE 资源文件读写（客户端图标等） |

许可证文本见 `Licenses/` 目录。

---

## 六、项目整体分层示意

```
┌─────────────────────────────────────────────────┐
│              Quasar.Server（控制端）              │
│  WinForms GUI → Messages → Networking（监听）    │
│  Build（客户端生成器）  Controls（自定义控件）    │
└──────────────┬──────────────────────────────────┘
               │  TCP + TLS + AES-256 + Protobuf
┌──────────────┴──────────────────────────────────┐
│              Quasar.Common（公共库）              │
│  Messages（协议定义）  Networking（帧收发）       │
│  Cryptography（加密）  Models（数据模型）         │
└──────────────┬──────────────────────────────────┘
               │
┌──────────────┴──────────────────────────────────┐
│              Quasar.Client（被控端）              │
│  Messages（Handler）  Setup（安装/启动）          │
│  Recovery（密码提取）  Logging（键盘记录）        │
│  ReverseProxy（SOCKS5）  Registry（注册表）      │
└─────────────────────────────────────────────────┘

         ↑↓ 由 Quasar.Common.Tests 覆盖测试
```

---

## 七、各目录职责速查表

| 目录/文件 | 职责 |
|-----------|------|
| `Quasar.sln` | 解决方案入口，管理四个子项目 |
| `Quasar.Common/Messages/` | 所有通信消息的数据结构定义 |
| `Quasar.Common/Cryptography/` | AES-256 加密与哈希工具 |
| `Quasar.Common/Networking/` | 底层帧封包/解包逻辑 |
| `Quasar.Client/Config/Settings.cs` | 客户端编译期配置（服务端地址、密钥等） |
| `Quasar.Client/Messages/` | 客户端各功能的消息响应处理器 |
| `Quasar.Client/Recovery/` | 浏览器与 FTP 客户端密码提取 |
| `Quasar.Client/Setup/` | 安装、卸载、自启、自更新逻辑 |
| `Quasar.Server/Forms/` | 管理员操作的图形界面窗体 |
| `Quasar.Server/Build/` | 客户端生成器（编译并注入配置） |
| `Quasar.Common.Tests/` | 公共库的 NUnit 单元测试 |
| `appveyor.yml` | AppVeyor CI 配置（自动构建） |
| `Licenses/` | 所有第三方依赖的许可证文本 |
